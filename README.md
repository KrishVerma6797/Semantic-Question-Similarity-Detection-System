# Semantic-Question-Similarity-Detection-System

This project detects duplicate questions using both classical NLP and transformer-based embeddings.

Models used:
- TF-IDF + Logistic Regression
- Sentence-BERT transformer embeddings

Pipeline:
Text → Embeddings → Similarity → Classification

Dataset: Quora Question Pairs